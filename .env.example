# From your GitHub OAuth App (Settings → Developer Settings → OAuth Apps)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
NEXT_PUBLIC_GITHUB_CLIENT_ID=   # same value as GITHUB_CLIENT_ID

# Generate with: openssl rand -base64 32
COOKIE_SECRET=
